schema:
  - ${REACT_APP_API_URL}
config:
  withHooks: true
  immutableTypes: true
  preResolveTypes: true
generates:
  src/api/model.graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  src/api/graphql.schema.json:
    plugins:
      - "introspection"
